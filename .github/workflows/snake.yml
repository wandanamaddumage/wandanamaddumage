name: Generate Contribution Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install svgwrite

      - name: Generate contribution snake with custom colors
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: rwandanamaddumage
          outputs: |
            dist/snake-base.svg

      - name: Apply cyber color theme
        run: |
          python << 'EOF'
          import re
          
          # Read the SVG
          with open('dist/snake-base.svg', 'r') as f:
              content = f.read()
          
          # Color mapping for GitHub contribution grid (your space theme)
          color_map = {
              '#ebedf0': '#0a0e27',
              '#9be9a8': '#30344d',
              '#40c463': '#004980',
              '#30a14e': '#0088bf',
              '#216e39': '#00c8ff',
          }
          
          # Replace colors
          for old, new in color_map.items():
              content = content.replace(old, new)
          
          # Add glow filter
          glow_filter = '''
          <defs>
              <filter id="glowSmall" x="-50%" y="-50%" width="200%" height="200%">
                  <feGaussianBlur stdDeviation="2" result="blur"/>
                  <feMerge>
                      <feMergeNode in="blur"/>
                      <feMergeNode in="SourceGraphic"/>
                  </feMerge>
              </filter>
          </defs>
          '''
          content = content.replace('<svg', '<svg' + glow_filter, 1)
          
          # Make snake glow cyan - FIXED VERSION
          content = re.sub(
              r'<path[^>]*class="[^"]*snake[^"]*"[^>]*>',
              lambda m: m.group(0).replace('fill="', 'fill="#00c8ff" filter="url(#glowSmall)" '),
              content
          )
          
          with open('dist/github-contribution-grid-snake-cyber.svg', 'w') as f:
              f.write(content)
          EOF

      - name: Push output to `output` branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}